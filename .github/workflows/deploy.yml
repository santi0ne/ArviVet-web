name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '18'

jobs:
  # Solo ejecutar deploy si todas las pruebas pasan
  deploy:
    name: Deploy Application
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
    needs: [] # Se puede configurar para depender de otros workflows
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run type checking
        run: npm run type-check

      - name: Run linting
        run: npm run lint

      - name: Run tests
        run: npm run test:ci

      - name: Build application
        run: npm run build

      - name: Generate deployment package
        run: |
          tar -czf deployment.tar.gz .next/ package.json package-lock.json public/ next.config.mjs

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: deployment.tar.gz
          retention-days: 30

      # Vercel deployment (uncomment if using Vercel)
      # - name: Deploy to Vercel
      #   uses: amondnet/vercel-action@v25
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #     vercel-args: '--prod'

      # AWS S3 deployment (uncomment if using AWS)
      # - name: Deploy to AWS S3
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
      #     AWS_REGION: 'us-east-1'
      #   run: |
      #     aws s3 sync .next/static s3://$AWS_S3_BUCKET/static
      #     aws s3 sync .next s3://$AWS_S3_BUCKET/_next

      - name: Deployment notification
        run: |
          echo "🚀 Deployment completed successfully!"
          echo "📦 Build artifacts uploaded"
          echo "🔗 Application URL: [Configure your deployment URL]"
